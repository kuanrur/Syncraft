/** Pair-friction threshold in minutes. If the requester's avg reply time to a sender exceeds this, the chip view surfaces a "you usually take a while to reply" hint. */
export const PAIR_FRICTION_THRESHOLD_MIN = 120;
